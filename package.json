{
    "name": "cataloging-playwright-tests",
    "private": true,
    "version": "1.0.0",
    "description": "Playwright browser tests for UI coverage not exercised by Panther",
    "scripts": {
        "mcp:chrome": "npx -y chrome-devtools-mcp@latest --headless --isolated --no-usage-statistics --no-performance-crux",
        "mcp:chrome:check": "npx -y chrome-devtools-mcp@latest --help",
        "test:playwright": "playwright test"
    },
    "devDependencies": {
        "@playwright/test": "^1.54.2"
    }
}

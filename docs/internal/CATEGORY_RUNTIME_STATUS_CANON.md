# Category Runtime Status Canon

- Category remains the root aggregate.
- Users remain external actors.
- Runtime status is a read-side surface, not a new ownership model.
- Governance and traceability stay category-local and actor-oriented.
- Workflow and review state are summarized without introducing direct ORM relations to external actors.
- Admin and CLI entrypoints read the same vendor-local style runtime contour for category governance.

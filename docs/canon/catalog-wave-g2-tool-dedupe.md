# Catalog Wave G2 — tool archive dedupe (minimal scope)

Base: current user slice `Cataloging.zip`

Authoritative location kept:
- `tools/ops-doc/*.zip`

Removed mirrored duplicates:
- `tools/Category/sketch7/*.zip`

Duplicate files removed: 19
All removed files were byte-identical to the retained `tools/ops-doc` copies.

# Catalog naming wave 2

Scope:
- promote `Catalog` as the component-level first token
- rename top-level component classes/files/docs/spec files from `Category...` to `Catalog...`
- keep route/documentation surface aligned with the component name

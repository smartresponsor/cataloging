# K13 Step04 — CLI delivery / retry / history inspection

Adds operator-facing Symfony Console commands for failed delivery inspection, retry scheduling, and destination history inspection. The step stays inside `src/Command` and reuses existing service contracts.

# Catalog naming wave 3

Scope:
- use `CatalogCategory...` where the context is an inner unit within a catalog
- keep `Category` as the subordinate concept, not the whole component
- align service/query/api layer names with the canon

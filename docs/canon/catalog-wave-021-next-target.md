# Cataloging wave 021

Next target after this bridge line:
- remove `src/Domain/*` wrappers after reference scan
- remove `src/ServiceInterface/Domaine/*` wrappers after namespace sweep
- collapse `src/Infra*` once service wiring points to `src/Infrastructure*`
- remove `config/config/*` shadow tree after config import scan

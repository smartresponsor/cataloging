# Catalog Wave D — tests and tools alignment

Base: `cataloging-203-current-repository-hotfix-stabilized.zip`

Scope:
- replace legacy `tests/Category/**` with canonical top-level test directories
- align test namespaces and first-token naming with current catalog canon
- refresh `phpunit.xml.dist` to use `tests`
- rename active linter scripts to canonical `Catalog...` names

cataloging next target wave d

current focus after 050:
- finish `App\\Layer*` to `App\\Service*` / `App\\ServiceInterface*`
- finish `App\\Http*` to `App\\Controller*` / `App\\ControllerInterface*`
- finish physical delete of legacy `Domain*`, `Infra*`, `Adapter*`, `config/config/*`
- collapse duplicate owner classes in seo/security/graphql/repository

recommended next batch:
1. service layer category utilities cutover
2. service layer security nested cutover
3. http namespace cutover for `src/Http/*`
4. delete-family execute dry-run reports
5. legacy wrapper shrink pass

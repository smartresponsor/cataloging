# Cataloging wave 025 — shadow config cutover prep

Canonical owner:
- `config/*`

Shadow tree:
- `config/config/*`

This wave does not delete shadow config files yet.
It adds a deterministic diff script and a delete-ready inventory for inspection-driven cleanup.

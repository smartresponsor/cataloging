# Cataloging wave 022 — InfrastructureInterface canon

Goal:
- introduce canonical `src/InfrastructureInterface/*`
- keep `src/InfraInterface/*` as compatibility bridge only

Scope:
- WebhookSenderInterface
- OutboxDispatcherInterface

Next:
- move implementations from `src/Infra/*` to `src/Infrastructure/*`
